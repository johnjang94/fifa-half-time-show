# FIFA Half-Time Show

Guest-only mobile invite web app.

Invite submissions are sent to `fifa-control`.

## Local setup

1. Copy `.env.example` to `.env.local`
2. Point `NEXT_PUBLIC_CONTROL_URL` at the backend
3. Run `npm run dev`
